## 1. 发布记录 API 客户端

- [x] 1.1 在 `src/modules/publication/` 中新增发布记录类型定义，与 `docs/api/publication.yaml` 100% 对齐
- [x] 1.2 新增 publication API 客户端，支持 `POST /api/publication-records`
- [x] 1.3 新增 publication API 客户端，支持 `GET /api/fruits/{fruitId}/publication-records`
- [x] 1.4 新增 publication API 客户端，支持 `GET /api/publication-records/{publicationRecordId}` 和 `PATCH /api/publication-records/{publicationRecordId}`
- [x] 1.5 增加前端 API 客户端测试，验证路径和请求方法与 `docs/api/publication.yaml` 对齐

## 2. 工作区发布验证交互

- [x] 2.1 将工作区果实详情底部的发布相关禁用按钮重构为可交互的发布记录区域
- [x] 2.2 仅在已选择果实且工作区非只读时展示人工发布记录创建入口
- [x] 2.3 为候选果实、已淘汰果实和只读工作区展示不可发布说明
- [x] 2.4 实现人工发布记录创建弹窗或抽屉，包含发布目标、发布凭证和发布备注
- [x] 2.5 创建发布记录成功后刷新当前果实的发布记录列表

## 3. 发布记录列表与编辑

- [x] 3.1 在果实详情中按需加载当前果实的发布记录列表
- [x] 3.2 展示发布目标、发布凭证、发布备注、发布器语义和发布时间
- [x] 3.3 实现发布记录空状态，并引导已选择果实创建人工发布记录
- [x] 3.4 实现发布记录编辑入口，只允许编辑发布目标、发布凭证和发布备注
- [x] 3.5 编辑发布记录成功后刷新该记录展示，并保持关联果实和发布时间不可编辑

## 4. 数据回流入口

- [x] 4.1 在每条发布记录下展示监控器和数据反馈入口，明确数据回流属于具体发布记录
- [x] 4.2 无发布记录时不展示反馈快照创建入口，并提示需要先创建发布记录
- [x] 4.3 【依赖后端更新】等待 `docs/api/feedback.yaml` 落地后，新增 `src/modules/feedback/` 类型与 API 客户端
- [x] 4.4 【依赖后端更新】接入人为监控器挂载接口，并在发布记录下展示挂载状态
- [x] 4.5 【依赖后端更新】接入反馈快照创建、编辑和反馈历史查询接口

## 5. 体验与状态处理

- [x] 5.1 为发布记录创建、编辑、列表加载和反馈入口加载增加 loading、error 和 retry 状态
- [x] 5.2 确保发布记录与反馈交互不影响内容树拖拽、节点选择、枝化生长和基因汲取入口
- [x] 5.3 确保所有写操作尊重 `workspaceReadOnly`
- [x] 5.4 按现有工作区视觉风格优化发布记录与反馈区域 UI，不新增营销式页面或独立管理页面

## 6. 测试与验证

- [x] 6.1 增加 publication API 客户端路径测试
- [x] 6.2 增加工作区发布记录入口可见性测试，覆盖已选择、候选、已淘汰和只读状态
- [x] 6.3 增加发布记录创建和编辑交互测试
- [x] 6.4 【依赖后端更新】在 feedback API 契约落地后增加 feedback API 客户端路径测试
- [x] 6.5 运行前端 typecheck、lint 和 test，确认变更通过验证
