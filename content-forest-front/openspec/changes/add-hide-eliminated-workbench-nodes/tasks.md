## 1. 视图状态与可见节点

- [x] 1.1 在工作区页面新增隐藏淘汰节点的前端运行时状态，默认展示全部节点
- [x] 1.2 基于完整节点集合派生当前可见节点集合，隐藏状态下过滤 `selectionState = eliminated` 的果实节点
- [x] 1.3 调整连接线计算逻辑，仅为当前可见节点之间的父子关系生成连接线
- [x] 1.4 处理当前选中节点被隐藏的兜底逻辑，优先切换到种子根节点

## 2. Header 交互入口

- [x] 2.1 在工作区 header 操作区新增“隐藏淘汰节点 / 显示淘汰节点”切换按钮
- [x] 2.2 让切换按钮沿用当前工作区 header 操作按钮视觉和交互样式
- [x] 2.3 切换隐藏状态后触发树形重新整理，并将可见内容树居中展示

## 3. 树形布局与运行时行为

- [x] 3.1 调整树形布局输入，使整理树形、初始加载、隐藏切换均基于可见节点集合计算
- [x] 3.2 保持完整节点数据不丢失，重新显示淘汰节点时可无损恢复
- [x] 3.3 确保生长中占位节点在隐藏淘汰节点时仍可见、参与布局并保留临时连接线
- [x] 3.4 确保隐藏和展示操作不调用任何果实状态修改接口、节点位置持久化接口或新增后端接口

## 4. 验证

- [x] 4.1 运行 `npm run typecheck`
- [x] 4.2 运行 `npm run lint`
- [x] 4.3 运行 `npm test`
- [x] 4.4 在浏览器中验证隐藏淘汰节点、显示淘汰节点、动态整理树形和连接线同步
- [x] 4.5 在浏览器中验证当前选中淘汰节点被隐藏时，详情面板能切换到可见节点
