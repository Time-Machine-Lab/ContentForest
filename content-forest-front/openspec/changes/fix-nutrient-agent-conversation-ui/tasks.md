## 1. 后端契约对齐

- [x] 1.1 【依赖后端更新】等待 `docs/api/nutrient.yaml` 定义种子级研究会话列表接口
- [x] 1.2 【依赖后端更新】等待 `docs/api/nutrient.yaml` 定义完整营养研究 SSE 事件类型
- [x] 1.3 更新前端 nutrient API/types，使会话列表、取消能力和 SSE 事件类型与 `docs/api/nutrient.yaml` 对齐

## 2. 会话恢复与切换

- [x] 2.1 在营养工作台打开时加载当前种子的研究会话列表
- [x] 2.2 新建种子级会话后立即更新会话列表，并支持刷新后恢复
- [x] 2.3 点击营养内容卡片时优先加载卡片绑定会话，没有会话时展示可输入空态
- [x] 2.4 增加会话切换交互，避免当前输入污染其他会话

## 3. 流式对话状态机

- [x] 3.1 将当前消息渲染逻辑拆为 user、assistant、thought、tool、depositable 等前端区块模型
- [x] 3.2 接入 thought delta，支持思考块展开/收起和完成状态
- [x] 3.3 接入 tool lifecycle/progress 事件，展示工具调用块或时间线项
- [x] 3.4 接入普通 message delta，确保 Assistant 正文实时追加显示
- [x] 3.5 接入 nutrient block delta 和最终 depositable block，完成临时成果卡到持久化成果卡的替换

## 4. 暂停、重试与错误恢复

- [x] 4.1 使用 AbortController 管理当前流式请求
- [x] 4.2 Agent 运行中将发送按钮切换为暂停按钮，并阻止重复提交
- [x] 4.3 暂停后标记当前回复块为已取消，并允许用户继续输入或重试
- [x] 4.4 流式失败时保留上一条输入，展示失败状态和重试入口

## 5. UI/UX 优化与验证

- [x] 5.1 按 `ui-ux-pro-max` 方向重做对话区、输入框、发送/暂停/重试按钮和消息块视觉
- [x] 5.2 优化 thought、tool 和可沉淀营养卡片的视觉层级，避免看起来像廉价调试日志
- [x] 5.3 修复新会话空态的大面积空白问题
- [x] 5.4 在桌面和窄屏视口验证营养工作台不重叠、不溢出、主要操作可用
- [x] 5.5 运行前端类型检查和相关测试
