## 1. 契约与依赖确认

- [x] 1.1 确认后端 `architect-branch-growth-pipeline-backend` 已更新 `docs/api/growth.yaml`
- [x] 1.2 确认工作区快照或果实详情中方向摘要字段已由后端契约定义；若未定义，标记为后端依赖
  - 当前 `docs/api/workspace.yaml` 与 `docs/api/fruit.yaml` 未定义果实静态方向摘要；前端不伪造字段，仅展示生成中任务 `attempt.mutationPlan.direction`。
- [x] 1.3 阅读 `docs/spec/DESIGN.md`、`docs/spec/前端开发规范文档.md` 和第二期规划 2.3 章节

## 2. 枝化输入框管线控件

- [x] 2.1 在枝化生成详情中加入搜索模式展示与选择控件
- [x] 2.2 在枝化生成详情中加入突变激进程度控件，使用保守、均衡、激进等语义值
- [x] 2.3 将搜索模式和突变激进程度映射到 `POST /api/growth-tasks`
- [x] 2.4 确保不再向用户展示或提交数字突变率

## 3. 生成路径图与方向展示

- [x] 3.1 扩展任务轮询状态读取，消费 `GET /api/growth-tasks/{taskId}` 返回的路径图
- [x] 3.2 实现生成中节点点击后的路径图视图
- [x] 3.3 根据轮询结果更新路径图步骤状态
- [x] 3.4 在果实节点或详情中展示探索方向摘要
  - 生成中任务展示 `mutationPlan.direction`；已生成果实方向摘要等待后端契约补充后再接入。
- [x] 3.5 确保任务完成后路径图不作为果实正式内容展示

## 4. 验证

- [x] 4.1 补充搜索模式和突变激进程度控件测试
- [x] 4.2 补充 `POST /api/growth-tasks` 参数映射测试
- [x] 4.3 补充路径图展示与轮询更新测试
- [x] 4.4 手动验证工作区画布、节点拖拽、输入框和物竞天择不受影响
- [x] 4.5 运行前端测试、类型检查、lint 和 OpenSpec 严格校验

## 5. 生成路径图展示语义修正

- [x] 5.1 只展示后端返回的用户可见路径步骤，并把路径图理解为用户任务进度
- [x] 5.2 不展示 `task_started`、`skill_called`、`tool_called`、`llm_called` 等工程事件
- [x] 5.3 在路径图顶部展示当前正在执行的用户任务
- [x] 5.4 优化子步骤缩进，例如“使用生成器”下展示“生成文案”“生成封面”“生成视频”
- [x] 5.5 补充测试，验证工程 trace 不会出现在 UI
