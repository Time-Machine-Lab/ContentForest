## 1. 后端契约确认

- [x] 1.1 【依赖后端更新】确认 `docs/api/nutrient.yaml` 中研究会话不再要求或展示 `nutrientCardId` 绑定语义
- [x] 1.2 【依赖后端更新】确认 `docs/api/nutrient.yaml` 中营养内容不再要求或展示 `conversationId` 绑定语义
- [x] 1.3 【依赖后端更新】等待 `docs/api/nutrient.yaml` 定义删除研究会话接口
- [x] 1.4 【依赖后端更新】确认可沉淀营养块保存为新草稿和合并到指定营养内容的接口契约
- [x] 1.5 更新前端 nutrient API/types，使会话删除、会话解耦和目标合并能力与 `docs/api/nutrient.yaml` 对齐

## 2. 工作台状态模型重构

- [x] 2.1 将营养工作台本地状态拆为当前模式、当前会话、当前营养内容、新会话草稿、建议浮层和合并目标选择器
- [x] 2.2 移除“点击营养内容自动加载会话”的前端路径
- [x] 2.3 实现点击新会话只进入草稿态，不立即创建后端会话
- [x] 2.4 实现新会话首条消息发送时创建会话并进入 SSE 流式对话
- [x] 2.5 保留当前 SSE 消息分块能力，包括 thought、tool、普通消息和可沉淀营养块

## 3. 会话栏 UI

- [x] 3.1 使用 `ui-ux-pro-max` 重新设计左侧会话栏，包含新会话入口、历史会话列表、选中态、更新时间和空态
- [x] 3.2 实现点击历史会话加载对话详情并切换主工作区模式
- [x] 3.3 实现会话删除按钮、确认交互、加载态和失败提示
- [x] 3.4 删除当前会话成功后回到新会话草稿态或空态，并刷新会话列表
- [x] 3.5 在 Agent 运行中禁用会话切换和删除，避免流式输出污染其他会话

## 4. 营养内容栏 UI

- [x] 4.1 使用 `ui-ux-pro-max` 重新设计右侧营养内容栏，包含本地搜索、状态筛选和资产卡片
- [x] 4.2 实现营养内容卡片点击后在主工作区展示详情，而不是进入会话
- [x] 4.3 根据草稿、正常、归档状态展示沉淀、归档、回档、默认带入、删除等可用操作
- [x] 4.4 对尚未被后端支持的删除或恢复能力展示清晰禁用态或隐藏入口
- [x] 4.5 保持营养内容搜索和筛选状态在会话切换时不被重置

## 5. 主工作区与建议浮层

- [x] 5.1 重构中间主工作区 header，使其展示当前模式、当前对象标题、建议消息按钮和关键操作
- [x] 5.2 将营养汲取建议从右栏迁移到 header 消息按钮触发的浮层
- [x] 5.3 实现建议浮层中的采纳和忽略操作
- [x] 5.4 采纳建议后切换到新会话草稿态，并将建议内容填入输入框
- [x] 5.5 优化空白对话态、营养内容详情态、会话加载态和错误态的视觉层级

## 6. 可沉淀营养块目标选择

- [x] 6.1 将成果卡操作文案从“合并到当前内容”改为“选择合并目标”
- [x] 6.2 实现保存为新草稿后刷新右侧营养内容栏
- [x] 6.3 实现合并目标选择器，支持本地搜索和状态过滤
- [x] 6.4 选择目标并确认后调用后端支持的合并或更新接口
- [x] 6.5 合并成功后刷新目标营养内容详情和右侧营养内容栏，并标记该成果卡已处理

## 7. 产品级 UI/UX 验证

- [x] 7.1 按 `ui-ux-pro-max` 检查所有按钮的默认、悬停、聚焦、加载、禁用和危险状态
- [x] 7.2 参考 Mobbin、Refero 的真实产品信息架构检查会话栏、主工作区和营养内容栏的可扫描性
- [x] 7.3 参考 SiteInspire、Godly、Lapa Ninja、CSS Design Awards、The FWA 的视觉层级，但避免营销页式装饰和无意义背景
- [x] 7.4 在桌面和窄屏视口验证布局不重叠、不溢出、输入框和主操作始终可访问
- [x] 7.5 更新静态测试、API 测试和类型检查，覆盖会话栏、营养内容栏、建议浮层和目标合并交互
- [x] 7.6 运行 `npm run typecheck` 和 `npm test`
