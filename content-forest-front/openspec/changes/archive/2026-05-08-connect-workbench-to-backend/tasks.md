## 1. API 模块

- [x] 1.1 新增工作区 API 类型与请求封装，对齐 `GET /api/seeds/{seedId}/workspace`
- [x] 1.2 新增果实 API 类型与请求封装，对齐详情、选择、淘汰、恢复候选接口
- [x] 1.3 新增枝化生长 API 类型与请求封装，对齐任务创建、任务查询、来源状态、失败输入和重试接口

## 2. 工作区数据接入

- [x] 2.1 将工作区页面的 Mock 树数据替换为工作区快照加载
- [x] 2.2 实现基于快照节点和边的运行时树布局与连接线绘制
- [x] 2.3 将快照资源映射为生成器、营养库和基因 @ 引用选项
- [x] 2.4 实现工作区加载、错误、空树和只读状态

## 3. 节点详情与物竞天择

- [x] 3.1 点击种子节点时加载种子详情 Markdown 并展示
- [x] 3.2 点击果实节点时加载果实详情 Markdown 并展示
- [x] 3.3 将选择、淘汰、恢复候选操作对接果实 API 并同步节点状态

## 4. 枝化生长

- [x] 4.1 将悬浮输入框提交对接 `POST /api/growth-tasks`
- [x] 4.2 实现来源节点生长中状态、任务轮询和完成后快照刷新
- [x] 4.3 实现最近失败输入恢复和重试入口

## 5. 验证

- [x] 5.1 为新增 API 模块补充请求路径和请求体测试
- [x] 5.2 执行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`
