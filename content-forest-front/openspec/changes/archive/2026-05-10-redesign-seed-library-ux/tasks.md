## 1. 页面结构与视觉基底

- [x] 1.1 重构 `/seeds` 页面为左侧种子索引、中间种子阅读器、右侧辅助信息区的三栏工作台结构
- [x] 1.2 基于 `docs/design/previews/seed-library-redesign-preview.html` 保留整体布局、深色网格背景、面板层级和低噪声视觉风格
- [x] 1.3 调整页面顶部标题、主操作和视图入口，使其符合 `docs/spec/DESIGN.md` 的 Quiet Command Workspace 风格
- [x] 1.4 实现响应式降级，使中窄屏下种子索引、阅读器和核心操作仍可顺序使用
- [x] 1.5 移除不必要的说明型页面文案，确保页面只保留完成当前任务所需信息

## 2. 种子索引区

- [x] 2.1 将种子列表改造为左侧索引区，保留未归档和已归档视图切换
- [x] 2.2 保持未归档视图调用 `GET /api/seeds`，已归档视图调用 `GET /api/seeds/archived`
- [x] 2.3 优化种子索引项样式，展示标题、状态、更新时间和简短摘要
- [x] 2.4 保留基于当前视图种子的搜索能力，不新增接口参数
- [x] 2.5 优化索引区加载、读取失败、空状态和搜索无结果状态
- [x] 2.6 确保选择索引项后调用 `GET /api/seeds/{seedId}` 并更新中央阅读器

## 3. 中央阅读器

- [x] 3.1 将种子详情 Markdown 从右侧窄栏迁移到中央宽幅阅读器展示
- [x] 3.2 使用统一 `MarkdownViewer` 渲染 `SeedDetail.markdown`，并优化正文宽度、行高、标题层级和内容容器
- [x] 3.3 在阅读器顶部展示当前种子标题、归档状态和更新时间
- [x] 3.4 让阅读器顶部标题和核心操作在长文档阅读时保持稳定可达
- [x] 3.5 确保详情读取失败时展示统一异常提示，并保持左侧索引区可用

## 4. 操作层级与工作区入口

- [x] 4.1 将 `打开工作区` 设计为阅读器顶部主操作
- [x] 4.2 确保打开工作区继续调用 `GET /api/seeds/{seedId}/root-node`
- [x] 4.3 将 `编辑` 调整为次级入口，避免与主操作同级竞争
- [x] 4.4 将 `归档/回档` 收敛为低频管理操作，不与打开工作区并列为同等优先级
- [x] 4.5 确保归档继续调用 `POST /api/seeds/{seedId}/archive`，回档继续调用 `POST /api/seeds/{seedId}/restore`
- [x] 4.6 确保归档、回档和打开工作区失败时保留当前页面状态并展示局部失败反馈

## 5. 编辑态

- [x] 5.1 将编辑体验改造为阅读器内的专门编辑面板，而不是在阅读正文下方混排表单
- [x] 5.2 编辑态展示标题输入和 Markdown 正文编辑区，并保留取消和保存操作
- [x] 5.3 保存编辑继续调用 `PATCH /api/seeds/{seedId}`，并使用返回的 `SeedDetail` 更新阅读器和索引区
- [x] 5.4 阻止空标题或空 Markdown 正文保存，并通过统一异常提示或字段反馈展示错误
- [x] 5.5 保存失败时保留用户未保存草稿，不将失败请求伪装为成功态

## 6. 辅助信息区

- [x] 6.1 在右侧辅助信息区展示从 Markdown 标题派生的文档目录
- [x] 6.2 确保文档目录只作为前端 UI 辅助，不要求后端返回新字段
- [x] 6.3 在辅助信息区展示 `SeedDetail.contentLocation`、`SeedDetail.rootNodeId` 和 `SeedDetail.updatedAt`
- [x] 6.4 当 Markdown 标题不足以生成目录时，隐藏目录或展示低噪声空状态
- [x] 6.5 确保系统事实不混入 Markdown 正文阅读流

## 7. 验证

- [x] 7.1 更新种子库相关静态测试，覆盖三栏结构、主操作层级、编辑态和归档/回档入口边界
- [x] 7.2 确认种子 API client 和页面调用仍与 `docs/api/seed.yaml` 对齐
- [x] 7.3 运行 `npm run typecheck`
- [x] 7.4 运行 `npm run lint`
- [x] 7.5 运行 `npm test`
- [x] 7.6 使用浏览器检查 `/seeds` 页面视觉结构、阅读器宽度、编辑态、更多菜单、归档视图和响应式表现

