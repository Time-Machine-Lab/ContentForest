## 1. 准备与约束对齐

- [ ] 1.1 阅读 `DESIGN.md` 和 `docs/spec/前端开发规范文档.md`，确认本次 UI 风格与前端分层约束
- [ ] 1.2 阅读 `docs/api/seed.yaml` 和 `docs/sql/seed.sql`，确认种子接口与前端类型映射
- [ ] 1.3 确认本次不实现搜索接口、排序接口、果实数量、最近生长快照和内容树画布

## 2. 工作台外壳

- [ ] 2.1 创建主工作台 layout，包含左侧导航、顶部命令区域和主内容容器
- [ ] 2.2 实现左侧导航入口，包含种子库、生成器、营养库，并确保不出现全局已归档入口
- [ ] 2.3 接入暗色主题基础样式和工作台布局样式，符合 Quiet Command Workspace 风格
- [ ] 2.4 为未实现的生成器、营养库入口提供合理占位或禁用态，不伪造功能完成状态

## 3. 种子 API 与状态封装

- [ ] 3.1 定义与 `docs/api/seed.yaml` 对齐的前端种子请求与响应类型
- [ ] 3.2 实现 `GET /api/seeds` 和 `GET /api/seeds/archived` 的访问封装
- [ ] 3.3 实现 `GET /api/seeds/{seedId}` 的详情访问封装
- [ ] 3.4 实现 `POST /api/seeds`、`PATCH /api/seeds/{seedId}`、`POST /api/seeds/{seedId}/archive`、`POST /api/seeds/{seedId}/restore` 的写操作封装
- [ ] 3.5 实现 `GET /api/seeds/{seedId}/root-node` 的工作区入口数据访问封装
- [ ] 3.6 统一处理种子 API 的加载、成功、失败和输入保留逻辑

## 4. 种子库页面

- [ ] 4.1 创建种子库页面入口，并默认展示未归档种子卡片网格
- [ ] 4.2 在种子库页面内部实现未归档/已归档视图切换，并分别调用对应列表接口
- [ ] 4.3 实现种子卡片组件，只展示接口契约中存在的信息
- [ ] 4.4 实现未归档列表和已归档列表的加载态、空状态和失败态
- [ ] 4.5 实现卡片选中后读取种子详情并打开右侧详情面板

## 5. 创建与编辑交互

- [ ] 5.1 实现居中 Command Modal 新建种子入口
- [ ] 5.2 在新建种子提交前校验标题和 Markdown 正文非空
- [ ] 5.3 创建成功后刷新当前视图或插入新种子，并选中新建种子
- [ ] 5.4 实现右侧详情面板的 Markdown 查看态和编辑态
- [ ] 5.5 实现种子标题和 Markdown 正文保存，保存失败时保留未保存输入

## 6. 归档、回档与工作区入口

- [ ] 6.1 在未归档种子详情中实现归档操作，并在成功后更新当前视图
- [ ] 6.2 在已归档种子详情中实现回档操作，并在成功后更新当前视图
- [ ] 6.3 实现归档和回档失败反馈，失败时不改变前端系统状态
- [ ] 6.4 实现打开工作区入口，调用根节点接口后进入工作区路由或占位视图
- [ ] 6.5 对已归档种子的工作区入口展示只读语义

## 7. 验证

- [ ] 7.1 添加种子 API 封装的单元测试或等价验证
- [ ] 7.2 添加种子库关键交互测试，覆盖列表切换、创建、编辑、归档和回档
- [ ] 7.3 运行 `npm run typecheck`
- [ ] 7.4 运行 `npm run lint`
- [ ] 7.5 运行 `npm run build`
- [ ] 7.6 使用浏览器人工走查种子库主路径与归档筛选位置
