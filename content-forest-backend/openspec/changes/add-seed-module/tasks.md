## 1. 顶层契约文档

- [ ] 1.1 在 `docs/sql/` 创建种子系统事实对应的 SQL 设计文档，覆盖种子身份、归档状态、根节点语义、内容本体位置和必要审计信息。
- [ ] 1.2 在 `docs/api/` 创建种子 Controller 对应的 OpenAPI 文档，覆盖创建、列表、详情、编辑、归档、回档和可生长性校验等应用能力。
- [ ] 1.3 校验 SQL 与 API 文档均能回溯到 `docs/design/domain/种子领域模块设计文档.md` 和本变更 spec。

## 2. 模块骨架与边界

- [ ] 2.1 创建后端 `seed` 模块目录结构，确保符合 `docs/spec/后端开发规范文档.md` 的模块边界。
- [ ] 2.2 定义种子模块应用服务入口，禁止控制器直接访问数据库、文件系统或 Agent。
- [ ] 2.3 定义种子模块所需的存储端口和内容访问端口依赖，保持业务逻辑与具体 SQLite/文件系统实现解耦。

## 3. 种子内容与系统事实

- [ ] 3.1 实现创建种子流程：校验标题和 Markdown 正文非空，保存 Markdown 内容本体，建立种子系统事实和根节点语义。
- [ ] 3.2 实现查看种子详情流程：读取系统事实并通过内容访问层读取 Markdown 正文。
- [ ] 3.3 实现编辑种子标题和 Markdown 正文流程，确保编辑不改变种子身份、根节点关系和历史关系。
- [ ] 3.4 实现创建和编辑时的完整性校验，禁止保存空标题或空 Markdown 正文。

## 4. 归档与列表能力

- [ ] 4.1 实现未归档种子列表，只返回主种子库应展示的种子。
- [ ] 4.2 实现已归档种子列表，只返回已归档页面应展示的种子。
- [ ] 4.3 实现归档种子能力，确保归档不是删除，并保留内容与历史关系。
- [ ] 4.4 实现回档种子能力，恢复主种子库展示和生长源资格。

## 5. 工作区与生长源边界

- [ ] 5.1 实现种子作为内容树根节点的读取能力，供工作区模块后续展示使用。
- [ ] 5.2 实现种子可作为生长源的判断能力：未归档种子可生长，已归档种子不可生长。
- [ ] 5.3 确保已归档种子工作区只读语义可被后续工作区和枝化生长模块复用。

## 6. 测试与验证

- [ ] 6.1 覆盖创建种子成功、标题为空失败、正文为空失败的测试。
- [ ] 6.2 覆盖未归档列表和已归档列表分类返回的测试。
- [ ] 6.3 覆盖编辑未归档和已归档种子内容的测试。
- [ ] 6.4 覆盖归档、回档和禁止删除语义的测试。
- [ ] 6.5 覆盖已归档种子不可作为生长源、回档后恢复生长源资格的测试。
- [ ] 6.6 执行类型检查、Lint、测试和构建，确认种子模块符合后端开发规范。
