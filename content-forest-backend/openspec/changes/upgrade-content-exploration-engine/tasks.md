## 1. 顶层文档与契约

- [x] 1.1 更新 `docs/api/growth.yaml`，为任务或 attempt 详情补充解空间摘要、选中探索路线、参考计划、突变算子、平台推断来源等 additive schema，并保留现有 `searchMode`、`mutationIntensity`、`mutationPlan`。
- [x] 1.2 更新 `docs/sql/growth.sql`，说明路线元数据存放在现有 JSON 字段或新增 additive JSON 字段中，不新增独立路线表。
- [x] 1.3 更新 `docs/design/domain/枝化生长领域模块设计文档.md`，将探索槽位从核心机制调整为兼容兜底，并补充内容解空间地图、探索路线、参考计划、突变算子和平台推断优先级。
- [x] 1.4 更新 `docs/design/内容森林Agent架构设计文档.md`，补充 AgentPort 与内置枝化生长 Skill 对路线元数据、参考计划、候选证据和路线级 Trace 的处理。
- [x] 1.5 更新 `docs/design/domain/生成器领域模块设计文档.md`，说明生成器可作为平台/方法论推断来源，但不要求专用 manifest，也不承担生长策略职责。
- [x] 1.6 更新 `docs/内容森林第二期开发规划文档.md`，把“探索模式/突变方向”表述升级为“解空间搜索/探索路线/突变算子”的产品语义。

## 2. 策略模型与管线

- [ ] 2.1 在 Growth 或 Agent 共享类型中定义 `ContentSearchMap`、`ExplorationRoute`、`ReferencePlan`、`MutationOperator`、`PlatformInference` 等轻量结构。
- [ ] 2.2 实现平台与内容形态推断逻辑，优先级为生成器线索、用户显式要求、系统上下文推断。
- [ ] 2.3 在枝化生长创作搜索层构建内容解空间地图，并在失败时保留现有动态突变计划兜底。
- [ ] 2.4 基于解空间地图生成候选探索路线，并按质量与差异性为每个 attempt 分配选中路线。
- [ ] 2.5 将选中路线、参考计划和突变算子写入 attempt 级策略输入或存储 JSON，保持历史任务兼容。
- [ ] 2.6 调整现有 `mutationPlan` 构建，使其从选中路线派生方向、意图、继承、规避和证据摘要。

## 3. Agent 与 Skill

- [ ] 3.1 扩展 Branch Growth AgentPort 输入，传递解空间摘要、选中探索路线、参考计划、突变算子和平台推断来源。
- [ ] 3.2 升级 `content-evolution-strategy.ts`，优先使用选中探索路线生成策略，并仅在缺少路线时使用固定探索槽位兜底。
- [ ] 3.3 让证据卡片覆盖正式营养、临时营养卡片、基因、来源节点和反馈语义，且临时营养卡片标记为候选/低置信证据。
- [ ] 3.4 更新 `branch-growth-skill.ts` 的 prompt 和受控脚本输入，使生成器 payload 生成遵循选中路线、参考计划和突变算子。
- [ ] 3.5 更新结构化候选果实提示和校验，允许返回路线摘要、路线标识、突变算子和风险提示等候选 meta，但禁止系统事实伪造。
- [ ] 3.6 补充路线级 Trace 和用户可理解路径步骤，区分用户进度与工程 Trace。

## 4. 测试与验证

- [ ] 4.1 添加平台推断优先级测试，覆盖生成器优先、用户显式要求、系统推断和推断失败降级。
- [ ] 4.2 添加探索路线生成与 attempt 分配测试，验证多果实路线差异性、单果实路线存在、固定槽位兜底。
- [ ] 4.3 添加参考计划和证据卡片测试，验证正式营养、临时营养卡片、基因正负向语义和授权边界。
- [ ] 4.4 添加 AgentPort/Skill 集成测试，验证路线元数据进入 prompt、脚本输入、metadata 和 Trace。
- [ ] 4.5 添加 API/存储序列化测试，验证新增路线字段向后兼容历史任务。
- [ ] 4.6 运行相关测试套件和 OpenSpec 校验，确认变更满足 specs 与顶层文档契约。
