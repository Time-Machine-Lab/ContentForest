## 1. 契约与结构化输出

- [x] 1.1 定义基因汲取结构化建议契约，覆盖标题、正文、正向/反向语义、证据解释、谱系、生态位和相似基因关系
- [x] 1.2 实现基因建议 Schema 校验器，覆盖建议数量 1 到 3 条、空标题、空正文、非法方向、非法相似关系、真实路径和系统事实伪造
- [x] 1.3 实现基因建议输出适配器，将 Agent 结构化建议转换为 GeneService 可消费的 `suggestions` 数组
- [x] 1.4 扩展 Agent 输出校验流程，使 `gene_extraction` 任务能执行基因建议专用结构校验

## 2. 基因汲取只读 Tool

- [x] 2.1 定义并实现种子上下文读取 Tool，读取任务所属种子的标题和 Markdown 正文
- [x] 2.2 定义并实现基因证据读取 Tool，支持读取果实选择和果实淘汰证据的 meta 与 Markdown 正文
- [x] 2.3 在基因证据读取 Tool 中接入发布记录读取，支持发布目标、发布凭证、发布备注和发布时间
- [x] 2.4 定义并实现可引用基因经验读取 Tool，读取任务输入中列出的未归档基因经验正文
- [x] 2.5 在 Tool 中实现授权边界校验，拒绝读取非任务种子、非任务证据、其他种子基因经验和真实绝对路径
- [x] 2.6 保留营养库与数据回流读取占位，明确返回暂不支持而不是伪造内容

## 3. 内置基因汲取 Skill

- [x] 3.1 实现内置基因汲取 Skill，并声明支持 Agent `gene_extraction` 任务类型
- [x] 3.2 在 Skill 中读取种子上下文、证据上下文和既有可引用基因经验
- [x] 3.3 在 Skill 中组织基因汲取 Prompt，明确区分正向基因、反向基因、证据强度和用户确认边界
- [x] 3.4 在 Skill 中要求单次返回 1 到 3 条建议，并提示相似基因关系但不自动合并
- [x] 3.5 确保 Skill 不调用 GeneService、不写文件、不写数据库、不修改提醒、任务、建议或基因经验状态

## 4. 结构化自检与修复

- [x] 4.1 实现基因建议结构化提交解析，支持 JSON 对象或等价结构化输出
- [x] 4.2 实现结构校验失败后的修复流程，将错误列表反馈给模型并要求只修复结构
- [x] 4.3 限制修复次数，修复失败后返回 Agent 失败结果
- [x] 4.4 将证据读取、建议生成、Schema 校验、修复重试和最终失败原因写入 Agent Trace
- [x] 4.5 确保 Trace 不包含 API Key、真实绝对路径或过长正文

## 5. Agent 装配与领域集成

- [x] 5.1 在后端应用装配中注册内置基因汲取 Skill
- [x] 5.2 注册基因汲取 Skill 所需只读 Tool，包括种子上下文、证据上下文、发布记录上下文和既有基因经验读取
- [x] 5.3 确保 GeneService 仍负责汲取任务状态、建议持久化、提醒处理、建议确认和基因经验 Markdown 写入
- [x] 5.4 确认本变更不新增 API 契约、不新增 SQL 契约、不修改营养库或数据回流模块

## 6. 测试与验证

- [x] 6.1 新增基因建议 Schema 校验测试，覆盖成功、数量越界、空字段、非法方向、真实路径和系统事实伪造
- [x] 6.2 新增种子上下文读取 Tool 测试，覆盖成功读取、非任务种子拒绝和无绝对路径泄露
- [x] 6.3 新增基因证据读取 Tool 测试，覆盖果实选择、果实淘汰、发布记录、未授权证据和暂不支持反馈证据
- [x] 6.4 新增可引用基因经验读取 Tool 测试，覆盖成功读取、其他种子拒绝、已归档排除和未授权拒绝
- [x] 6.5 新增基因汲取 Skill 成功生成建议测试，验证正向/反向语义和相似基因关系进入可消费建议
- [x] 6.6 新增结构化输出自检修复测试，覆盖首次失败后修复成功和多次失败后返回失败
- [x] 6.7 新增 GeneService 集成测试，验证真实 Agent Runtime 返回建议后由 GeneService 保存为待确认建议
- [x] 6.8 新增边界测试，验证基因汲取 Skill 不直接保存建议、不写 Markdown、不写数据库、不确认基因经验
- [x] 6.9 运行 `npm run typecheck`、`npm run lint`、`npm test` 和 `npm run build`
