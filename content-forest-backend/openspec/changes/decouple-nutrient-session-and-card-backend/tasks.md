## 1. 顶层契约与文档

- [x] 1.1 更新 `docs/api/nutrient.yaml`，移除研究会话与营养内容绑定契约，新增删除研究会话接口，并明确可沉淀营养块保存/合并必须显式提供目标
- [x] 1.2 更新 `docs/sql/nutrient.sql`，移除或废弃 `nutrient_cards.conversation_id` 与 `nutrient_research_sessions.nutrient_card_id` 绑定字段语义，并调整相关索引说明
- [x] 1.3 更新 `docs/design/domain/营养库领域模块设计文档.md`，同步“会话是研究过程，营养内容是资料资产”的领域边界
- [x] 1.4 更新 `docs/design/内容森林架构设计文档.md` 与 `docs/内容森林第二期开发规划文档.md`，删除“营养内容关联会话”的旧工作台表述

## 2. API 与类型契约实现

- [x] 2.1 调整营养研究会话创建输入，停止接收或写入 `nutrientCardId`
- [x] 2.2 调整种子研究会话列表查询，移除 `nutrientCardId` 过滤语义，返回种子下全部会话
- [x] 2.3 调整营养内容详情和列表输出，移除 `conversationId` 字段
- [x] 2.4 新增 `DELETE /api/nutrient-research-sessions/{sessionId}` 路由和 Controller 方法
- [x] 2.5 调整可沉淀营养块保存/合并入口，保存为新草稿与合并到目标内容必须使用显式请求参数

## 3. 应用服务与存储实现

- [x] 3.1 更新 Nutrient 应用服务，创建会话时只校验种子归属，不再解析或绑定营养内容
- [x] 3.2 实现删除研究会话服务方法，删除会话、消息和可沉淀块，并确保不影响营养内容
- [x] 3.3 为运行中的流式研究会话增加删除保护，运行中请求删除时返回业务错误
- [x] 3.4 更新存储端口，移除会话按营养内容查询能力，新增删除研究会话及其子记录能力
- [x] 3.5 更新 SQLite 与 In-Memory 存储适配器，使查询、创建、删除行为与新契约一致
- [x] 3.6 清理卡片会话绑定服务、绑定路由和旧字段写入逻辑

## 4. 兼容与迁移

- [x] 4.1 对旧数据中的会话绑定字段采取忽略或一次性清理策略，并在测试中确认旧字段不会出现在新 API 响应中
- [x] 4.2 确认删除会话后，已经保存为草稿、已沉淀或已归档的营养内容仍可正常查询、编辑、归档和引用
- [x] 4.3 确认可沉淀营养块合并时目标内容必须属于同一种子且未归档

## 5. 测试与验证

- [x] 5.1 更新 `nutrient-service` 单元测试，覆盖独立会话创建、列表、删除和删除不影响营养内容
- [x] 5.2 更新 HTTP/集成测试，覆盖删除会话接口、旧绑定字段移除和显式合并目标校验
- [x] 5.3 更新 SSE 相关测试，覆盖运行中会话拒绝删除或取消后允许删除
- [x] 5.4 运行后端类型检查和完整测试套件

