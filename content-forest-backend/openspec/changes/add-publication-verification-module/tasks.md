## 1. 顶层契约

- [ ] 1.1 创建 `docs/sql/publication.sql`，定义发布记录系统事实结构，并与发布验证领域规则对齐。
- [ ] 1.2 创建 `docs/api/publication.yaml`，定义 Publication Controller 的创建、编辑、查看和按果实列出发布记录能力。
- [ ] 1.3 校对 `docs/sql/publication.sql`、`docs/api/publication.yaml` 与 OpenSpec 规格，确保字段语义、不可编辑发布时间、无删除归档能力一致。

## 2. 领域与端口

- [ ] 2.1 创建 `src/modules/publication/` 模块结构和 `index.ts` 出口。
- [ ] 2.2 定义发布验证领域类型，覆盖发布记录、人工发布器语义、发布目标、发布凭证、发布备注和发布时间。
- [ ] 2.3 定义果实可发布校验端口，用于在创建发布记录前校验果实是否可发布。
- [ ] 2.4 定义发布记录存储端口，覆盖创建、保存、按 ID 查询、按果实查询和存在性校验能力。

## 3. 应用服务

- [ ] 3.1 实现 PublicationService 创建发布记录能力，调用果实可发布校验端口，并固定使用人工发布器语义。
- [ ] 3.2 实现 PublicationService 编辑发布记录能力，只允许编辑发布目标、发布凭证和发布备注，保持关联果实与发布时间不变。
- [ ] 3.3 实现 PublicationService 查看单条发布记录能力。
- [ ] 3.4 实现 PublicationService 按果实列出发布记录能力，支持一个果实多条发布记录。
- [ ] 3.5 实现 PublicationService 发布记录存在性校验能力，供后续数据回流模块使用。
- [ ] 3.6 为发布验证服务补齐参数校验、资源不存在错误和果实不可发布错误。

## 4. 存储实现

- [ ] 4.1 实现内存发布记录存储适配器，用于单元测试。
- [ ] 4.2 实现 SQLite 发布记录存储适配器，并与 `docs/sql/publication.sql` 保持一致。
- [ ] 4.3 在应用启动或存储初始化流程中装配发布记录存储适配器。

## 5. HTTP 接口

- [ ] 5.1 实现 Publication Controller，对齐 `docs/api/publication.yaml`。
- [ ] 5.2 在 HTTP 路由装配中注册发布验证接口。
- [ ] 5.3 确保接口错误响应沿用统一 ApplicationError 映射，不暴露底层异常。

## 6. 测试

- [ ] 6.1 添加发布记录创建成功测试，验证可发布果实、人工发布器语义和发布时间跟随创建时间。
- [ ] 6.2 添加果实不可发布时拒绝创建发布记录测试。
- [ ] 6.3 添加编辑发布记录测试，验证目标、凭证、备注可编辑，关联果实和发布时间不可变。
- [ ] 6.4 添加查看单条发布记录和按果实列出多条发布记录测试。
- [ ] 6.5 添加发布记录存在性校验测试，为后续数据回流入口保留保障。
- [ ] 6.6 添加不支持删除、归档、截图附件上传和平台自动发布的边界测试或静态契约检查。
- [ ] 6.7 添加 HTTP Controller 集成测试，覆盖 `docs/api/publication.yaml` 中的核心接口。

## 7. 验证

- [ ] 7.1 运行后端类型检查，确保新增模块类型通过。
- [ ] 7.2 运行后端测试，确保发布验证相关测试和现有种子、生成器测试通过。
- [ ] 7.3 运行 lint 或项目既有检查命令，确保代码风格符合后端开发规范。
