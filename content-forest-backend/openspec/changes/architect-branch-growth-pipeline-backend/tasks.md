## 1. 顶层文档与契约先行

- [x] 1.1 更新 `docs/内容森林第二期开发规划文档.md`，确认管线层、本轮生长简报、突变激进程度和路径图边界
- [x] 1.2 更新 `docs/design/domain/枝化生长领域模块设计文档.md`，补充六层管线、搜索模式、突变激进程度和路径图语义
- [x] 1.3 更新 `docs/design/内容森林Agent架构设计文档.md`，补充 AgentPort 本轮生长简报、突变计划和步骤上报契约
- [x] 1.4 更新 `docs/api/growth.yaml`，定义搜索模式、突变激进程度、生成路径图和任务状态响应字段
- [x] 1.5 更新 `docs/sql/growth.sql`，定义管线参数、路径图或 trace 摘要所需系统事实

## 2. 管线编排实现

- [x] 2.1 在 Growth 应用层拆分输入层、上下文补全层、创作搜索层、注意力编排层、生成执行层和结果封装层
- [x] 2.2 实现本轮生长简报组装，支持缺少种子主简报时降级
- [x] 2.3 实现搜索模式和突变激进程度的请求解析、缺省推荐和校验
- [x] 2.4 实现动态突变方向发现，确保不写死固定突变维度枚举
- [x] 2.5 为每个 attempt 生成可区分的突变计划和方向摘要

## 3. AgentPort 与路径图

- [x] 3.1 调整 AgentPort 枝化生长输入，传递本轮生长简报、搜索模式、突变激进程度和 attempt 突变计划
- [x] 3.2 实现管线固定大阶段路径图初始化
- [x] 3.3 支持 Agent 或生成器执行阶段追加子步骤和更新步骤状态
- [x] 3.4 在 `GET /api/growth-tasks/{taskId}` 中返回生成中路径图
- [x] 3.5 确保路径图不写入果实 Markdown，不影响物竞天择状态

## 4. 验证

- [x] 4.1 补充本轮生长简报组装测试
- [x] 4.2 补充搜索模式和突变激进程度默认推荐测试
- [x] 4.3 补充动态突变方向和 attempt 差异化测试
- [x] 4.4 补充路径图初始化、更新和查询测试
- [x] 4.5 运行后端测试、类型检查、lint 和 OpenSpec 严格校验
