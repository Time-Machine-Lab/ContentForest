## 1. 顶层契约文档

- [ ] 1.1 在 `docs/sql/` 创建果实系统事实对应的 SQL 设计文档，覆盖果实身份、选择状态、父节点引用、内容本体位置、摘要、基因标签和必要审计信息。
- [ ] 1.2 在 `docs/api/` 创建果实 Controller 对应的 OpenAPI 文档，覆盖详情读取、正文编辑、选择、淘汰、恢复候选等公开应用能力，并明确不提供公开创建和全局列表接口。
- [ ] 1.3 校验 SQL 与 API 文档均能回溯到 `docs/design/domain/果实领域模块设计文档.md` 和本变更 spec。

## 2. 模块骨架与边界

- [ ] 2.1 创建后端 `fruit` 模块目录结构，确保符合 `docs/spec/后端开发规范文档.md` 的模块边界。
- [ ] 2.2 定义果实领域类型，覆盖果实身份、选择状态、父节点引用、内容位置、摘要、基因标签、详情和摘要视图。
- [ ] 2.3 定义果实模块应用服务入口，禁止控制器直接访问数据库、文件系统或 Agent。
- [ ] 2.4 定义果实模块所需的存储端口和内容访问端口依赖，保持业务逻辑与具体 SQLite/文件系统实现解耦。

## 3. 内容访问与存储适配

- [ ] 3.1 实现果实 Markdown 内容访问端口，支持创建、读取和更新果实正文。
- [ ] 3.2 实现本地果实 Markdown 内容访问适配器，保存内容到 `data/content/fruits/`。
- [ ] 3.3 实现果实存储端口，支持创建、按身份查找、保存、按父节点查询子果实。
- [ ] 3.4 实现内存果实存储适配器，用于服务测试。
- [ ] 3.5 实现 SQLite 果实存储适配器，并与 `docs/sql/fruit.sql` 保持一致。
- [ ] 3.6 更新运行时目录初始化，确保果实内容目录可用且重复启动保持幂等。

## 4. 果实创建与详情能力

- [ ] 4.1 实现内部从果实候选创建果实流程，校验 Markdown 正文和父节点引用必备。
- [ ] 4.2 实现创建果实时保存 Markdown 正文、建立系统事实和父节点引用，并默认状态为候选。
- [ ] 4.3 实现创建失败时的内容文件清理，避免数据库写入失败后留下孤儿 Markdown。
- [ ] 4.4 实现查看果实详情流程，读取系统事实并通过内容访问层读取 Markdown 正文。
- [ ] 4.5 确保摘要允许为空、基因标签允许为空集合，详情读取能稳定返回。

## 5. 正文编辑与物竞天择状态

- [ ] 5.1 实现编辑果实 Markdown 正文能力，拒绝空正文，并确保不改变果实身份、父节点引用和选择状态。
- [ ] 5.2 实现选择果实能力，允许从候选或已淘汰等状态切换为已选择。
- [ ] 5.3 实现淘汰果实能力，确保淘汰不删除内容、不破坏父节点关系。
- [ ] 5.4 实现恢复候选能力，允许已选择或已淘汰果实回到候选状态。
- [ ] 5.5 确保后端不实现 `deleteFruit` 或任何硬删除果实能力。

## 6. 内部协作能力

- [ ] 6.1 实现按父节点查询子果实的内部应用能力，供后续内容树工作区模块使用。
- [ ] 6.2 确保不提供面向用户的全局果实列表 API。
- [ ] 6.3 实现果实可发布判断能力，只有已选择果实返回可发布。
- [ ] 6.4 实现果实作为生长源的基础引用能力，但不判断生长锁。

## 7. HTTP 控制器与应用装配

- [ ] 7.1 创建果实 HTTP 控制器，暴露详情读取、正文编辑、选择、淘汰、恢复候选等接口。
- [ ] 7.2 确保果实 HTTP 控制器不暴露创建果实接口和全局列表接口。
- [ ] 7.3 更新应用启动装配，将果实服务、存储适配器、内容访问适配器和控制器接入后端应用。
- [ ] 7.4 确保所有 HTTP 行为与 `docs/api/fruit.yaml` 对齐。

## 8. 测试与验证

- [ ] 8.1 覆盖内部创建果实成功、正文为空失败、父节点缺失失败的服务测试。
- [ ] 8.2 覆盖果实详情读取和 Markdown 内容读取的测试。
- [ ] 8.3 覆盖编辑果实正文成功、空正文失败、已淘汰果实仍可编辑的测试。
- [ ] 8.4 覆盖选择、淘汰、恢复候选、已淘汰后重新选择的状态切换测试。
- [ ] 8.5 覆盖禁止删除果实和不提供公开创建/全局列表接口的测试。
- [ ] 8.6 覆盖按父节点查询子果实、可发布判断、生长源基础引用的测试。
- [ ] 8.7 覆盖 SQLite 适配器与内容访问适配器的集成测试。
- [ ] 8.8 执行类型检查、Lint、测试和构建，确认果实模块符合后端开发规范。
