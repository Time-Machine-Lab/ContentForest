## 1. 授权与装配

- [x] 1.1 确认本变更不新增或修改 `docs/api` 与 `docs/sql` 契约
- [x] 1.2 在枝化生长任务创建阶段接入营养引用可引用性校验，拒绝归档、越权或不存在的营养引用
- [x] 1.3 确保生长任务 `authorizationScope` 只保留通过校验的 `nutrientRefs`
- [x] 1.4 在后端应用装配中为枝化生长资源读取 Tool 注入营养库 storage 与 Markdown content access

## 2. Agent 只读 Tool

- [x] 2.1 扩展枝化生长资源读取 Tool，读取已授权营养内容的标题、所属营养库摘要和 Markdown 正文
- [x] 2.2 确保 Tool 只读取任务授权范围内的营养引用，不允许 Agent 伪造任意营养 ID
- [x] 2.3 确保 Tool 返回结果不包含真实本地绝对路径或不必要的数据库状态字段
- [x] 2.4 保持 Tool 只读，不写文件、不写数据库、不修改营养库、果实、生长任务或基因状态

## 3. 枝化生长 Skill 集成

- [x] 3.1 确保枝化生长 Skill 将 Tool 返回的 `nutrients` 纳入生成器执行上下文
- [x] 3.2 确保生成器脚本执行路径也能接收授权营养资源上下文
- [x] 3.3 确保候选果实结构化输出仍只能引用本次授权的营养和基因资源

## 4. 测试与验证

- [x] 4.1 新增或补齐资源 Tool 测试，覆盖公共营养内容读取
- [x] 4.2 新增或补齐资源 Tool 测试，覆盖当前种子专属营养内容读取
- [x] 4.3 新增或补齐授权边界测试，覆盖其他种子专属营养、归档营养库、归档营养内容和未授权营养引用
- [x] 4.4 新增或补齐枝化生长 Skill 测试，验证营养资源进入生成器 prompt 或脚本输入
- [x] 4.5 新增或补齐边界测试，验证 Agent Tool 不写入营养库、不写 Markdown、不修改系统事实
- [x] 4.6 运行 `npm run typecheck`、`npm run lint`、`npm test` 和 `npm run build`
