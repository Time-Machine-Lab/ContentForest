## 1. 顶层契约文档

- [x] 1.1 在 `docs/api/` 创建工作区 Controller 对应的 OpenAPI 文档，覆盖工作区快照读取接口。
- [x] 1.2 明确该变更不新增工作区数据库表，不创建 `docs/sql/workspace.sql`。
- [x] 1.3 校验工作区 API 契约与 `workspace-aggregation` spec、工作区设计文档和现有领域接口边界一致。

## 2. 模块骨架与依赖边界

- [x] 2.1 创建后端 `workspace` 模块结构，包含应用服务、领域类型和模块导出入口。
- [x] 2.2 定义工作区聚合服务入口，禁止 Controller 直接访问数据库、文件系统或跨领域底层实现。
- [x] 2.3 接入种子、果实、枝化生长、生成器、营养库和基因库等现有领域服务作为只读聚合依赖。

## 3. 工作区快照聚合

- [x] 3.1 实现根据种子读取工作区快照的应用服务流程，包含种子根节点和工作区只读状态。
- [x] 3.2 实现内容树节点摘要和父子边关系聚合，支持从种子根节点递归读取多层果实。
- [x] 3.3 实现节点生长状态聚合，供前端展示生长动效和禁用重复生长入口。
- [x] 3.4 实现节点最近失败输入存在性聚合，供前端提示或恢复上次失败输入。
- [x] 3.5 实现工作区可引用资源聚合，包含可用生成器、可引用营养内容和可引用基因洞察。
- [x] 3.6 确保快照不返回果实 Markdown 正文、种子 Markdown 正文或发布记录全量详情。

## 4. HTTP 接口

- [x] 4.1 创建工作区 HTTP Controller，并保证接口实现与 `docs/api/workspace.yaml` 对齐。
- [x] 4.2 将工作区 Controller 注册到应用 HTTP 路由中。
- [x] 4.3 处理种子不存在、领域依赖读取失败和聚合异常等错误响应。

## 5. 测试与验证

- [x] 5.1 覆盖未归档种子读取工作区快照的测试。
- [x] 5.2 覆盖归档种子读取工作区快照并返回只读状态的测试。
- [x] 5.3 覆盖空内容树、多层果实树和父子边关系聚合测试。
- [x] 5.4 覆盖节点生长状态和最近失败输入提示聚合测试。
- [x] 5.5 覆盖可用生成器、可引用营养内容和可引用基因洞察聚合测试。
- [x] 5.6 执行类型检查、lint、测试和构建，确认工作区聚合模块符合后端开发规范。
