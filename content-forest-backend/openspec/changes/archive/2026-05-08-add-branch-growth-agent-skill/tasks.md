## 1. 契约与结构化输出

- [x] 1.1 定义候选果实结构化输出契约，覆盖果实 Markdown、生成器原始 payload、摘要、基因标签、资源引用和警告信息
- [x] 1.2 实现候选果实 Schema 校验器，覆盖空 Markdown、空摘要、非法基因标签、越权资源引用、真实路径和系统事实伪造
- [x] 1.3 实现候选果实提交结果适配器，将 AgentTaskOutput 中的结构化结果转换为 Growth 领域可消费的候选果实
- [x] 1.4 扩展 Agent 输出校验流程，使 `growth` 任务能够执行候选果实专用结构校验

## 2. 生成器 Skill 读取能力

- [x] 2.1 定义生成器 Skill 文件夹读取 Tool 契约，支持读取 `SKILL.md`、目录清单和附件摘要
- [x] 2.2 实现生成器 Skill 文件夹读取 Tool，基于任务授权范围读取生成器内容
- [x] 2.3 确保生成器读取 Tool 不向 Agent 暴露真实绝对路径
- [x] 2.4 覆盖生成器不存在、停用、未授权和缺少 `SKILL.md` 的失败行为

## 3. 生成器脚本受控执行

- [x] 3.1 定义生成器 JS 脚本执行 Tool 契约，限定输入、输出、入口脚本、超时和输出大小
- [x] 3.2 实现受控 JS 脚本执行 Tool，仅允许执行生成器 Skill 文件夹内授权脚本
- [x] 3.3 实现脚本执行失败包装，覆盖超时、异常退出、输出过大和非结构化输出
- [x] 3.4 确保脚本执行不能访问任意系统路径、不能接收真实 API Key、不能绕过任务授权范围

## 4. 内置枝化生长 Skill

- [x] 4.1 实现内置枝化生长 Skill，并声明支持 Agent `growth` 任务类型
- [x] 4.2 在枝化生长 Skill 中通过 Tool 读取来源节点内容、生成器 Skill、授权营养和授权基因上下文
- [x] 4.3 在枝化生长 Skill 中编排生成器 Skill：支持只基于 `SKILL.md` 生成 payload，也支持调用受控 JS 脚本生成 payload
- [x] 4.4 在枝化生长 Skill 中将生成器 payload 包装为候选果实结构化结果
- [x] 4.5 确保枝化生长 Skill 不调用 FruitService、不写文件、不写数据库、不修改 Growth 任务状态

## 5. 结构化自检与修复

- [x] 5.1 实现 Tool Calling 风格的候选果实提交机制或等价结构化提交适配
- [x] 5.2 实现结构校验失败后的修复流程，将错误列表反馈给模型并要求只修复结构
- [x] 5.3 限制修复次数，修复失败后返回 Agent 失败结果
- [x] 5.4 将候选提交、Schema 校验、修复重试和最终失败原因写入 Agent Trace
- [x] 5.5 确保 Trace 不包含 API Key、真实绝对路径或过长正文

## 6. Agent 装配与 Growth 集成

- [x] 6.1 在后端应用装配中注册内置枝化生长 Skill
- [x] 6.2 注册枝化生长 Skill 所需 Tool，包括生成器读取、生成器脚本执行和已有/新增只读上下文 Tool
- [x] 6.3 更新 Growth 领域 AgentPort 结果消费逻辑，使其只接受通过校验的候选果实结构
- [x] 6.4 确保 Growth 领域仍负责 FruitService 落地、尝试记录、任务状态、生长锁和失败重试输入

## 7. 测试与验证

- [x] 7.1 新增候选果实 Schema 校验测试，覆盖成功、空 Markdown、非法引用、真实路径和系统事实伪造
- [x] 7.2 新增生成器 Skill 读取 Tool 测试，覆盖成功读取、未授权、停用和缺少 `SKILL.md`
- [x] 7.3 新增生成器 JS 脚本执行 Tool 测试，覆盖成功、越权路径、超时、异常退出和输出过大
- [x] 7.4 新增枝化生长 Skill 成功生成候选果实测试，验证生成器 payload 被包装为结构化结果
- [x] 7.5 新增结构化输出自检修复测试，覆盖首次失败后修复成功和多次失败后返回失败
- [x] 7.6 新增 Growth 集成测试，验证 Agent 返回候选果实后由 Growth 调用 FruitService 落地
- [x] 7.7 新增边界测试，验证枝化生长 Skill 不直接保存果实、不写文件、不写数据库
- [x] 7.8 运行 `npm run typecheck`、`npm run lint`、`npm test` 和 `npm run build`
