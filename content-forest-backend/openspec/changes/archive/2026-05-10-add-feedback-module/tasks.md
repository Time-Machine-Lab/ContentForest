## 1. 顶层契约

- [x] 1.1 创建 `docs/api/feedback.yaml`，定义 Feedback Controller 的监控器挂载、反馈快照创建、反馈快照编辑和发布记录反馈历史查询接口
- [x] 1.2 创建 `docs/sql/feedback.sql`，定义监控器挂载和反馈快照的持久化结构
- [x] 1.3 校对 `docs/api/feedback.yaml` 与 `docs/sql/feedback.sql`，确保字段语义与 `docs/design/domain/数据回流领域模块设计文档.md` 一致

## 2. 领域与应用服务

- [x] 2.1 在 `src/modules/feedback/` 中定义数据回流领域类型，包括人为监控器、监控器挂载、反馈快照和反馈历史
- [x] 2.2 定义发布记录存在性校验端口，并通过发布验证模块能力进行适配
- [x] 2.3 实现反馈应用服务：挂载人为监控器、拒绝重复挂载、创建反馈快照、编辑反馈快照、查询反馈历史
- [x] 2.4 确保反馈服务不删除快照、不修改发布记录、不修改果实状态、不自动触发基因汲取

## 3. 存储适配

- [x] 3.1 新增反馈存储端口，覆盖监控器挂载和反馈快照读写能力
- [x] 3.2 新增内存反馈存储适配器，用于单元测试
- [x] 3.3 新增 SQLite 反馈存储适配器，并与 `docs/sql/feedback.sql` 100% 对齐
- [x] 3.4 在应用启动和关闭流程中装配反馈存储适配器

## 4. HTTP 接口

- [x] 4.1 新增 Feedback Controller，并保证接口路径、请求体、响应体与 `docs/api/feedback.yaml` 100% 对齐
- [x] 4.2 在 HTTP 路由中注册反馈相关接口
- [x] 4.3 为反馈接口补齐请求体验证、未知字段拒绝和错误响应映射

## 5. 基因汲取上下文

- [x] 5.1 扩展基因证据读取能力，使 `feedback` 类型证据可读取反馈快照上下文
- [x] 5.2 在反馈证据上下文中包含发布记录、监控器语义、表现数据和用户观察
- [x] 5.3 保证创建或编辑反馈快照不会自动创建基因汲取任务或正式基因经验

## 6. 测试与验证

- [x] 6.1 增加反馈应用服务单元测试，覆盖挂载、重复挂载、快照创建、快照编辑、反馈历史查询和禁止删除语义
- [x] 6.2 增加反馈 HTTP 集成测试，覆盖 `docs/api/feedback.yaml` 定义的核心接口
- [x] 6.3 增加反馈 SQLite 存储测试，验证持久化结构与查询行为
- [x] 6.4 增加基因证据读取测试，覆盖 `feedback` 证据上下文读取
- [x] 6.5 运行后端 typecheck、lint 和 test，确认变更通过验证
