## 1. 文档同步

- [x] 1.1 更新 `docs/design/内容森林架构设计文档.md`，补充运行时内容根目录、标准目录骨架和 Bootstrap 职责。
- [x] 1.2 更新 `docs/spec/后端开发规范文档.md`，补充启动目录初始化、相对内容位置和禁止数据库保存绝对路径规则。
- [x] 1.3 确认本变更不涉及新增或修改 `docs/api/*.yaml` 与 `docs/sql/*.sql`。

## 2. 配置与 Bootstrap

- [x] 2.1 在 `src/app/config` 定义应用运行配置读取能力，覆盖内容根目录和 SQLite 数据库路径。
- [x] 2.2 在 `src/app/bootstrap` 实现运行时文件系统初始化能力，创建内容根目录和标准子目录。
- [x] 2.3 确保 Bootstrap 初始化过程幂等，不清空或覆盖已有内容文件。

## 3. 内容访问适配器

- [x] 3.1 调整本地 Markdown 内容访问适配器，使其基于统一内容根目录解析相对内容位置。
- [x] 3.2 确保内容访问适配器拒绝绝对路径、越界相对路径和越过内容根目录的解析结果。
- [x] 3.3 确保种子 Markdown 内容位置保存为 `seeds/*.md` 等相对路径。

## 4. 测试与验证

- [x] 4.1 覆盖首次启动创建目录骨架的测试。
- [x] 4.2 覆盖重复启动不破坏已有文件的测试。
- [x] 4.3 覆盖内容位置为相对路径且拒绝绝对路径的测试。
- [x] 4.4 覆盖内容访问适配器拒绝路径越界的测试。
- [x] 4.5 执行类型检查、Lint、测试和构建，确认启动文件系统能力符合后端开发规范。
