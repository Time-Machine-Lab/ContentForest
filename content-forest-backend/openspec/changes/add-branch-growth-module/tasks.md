## 1. 顶层契约文档

- [x] 1.1 创建 `docs/api/growth.yaml`，覆盖发起枝化生长、重试最近失败任务、查询任务状态、查询节点生长状态、查询最近失败输入等 Growth Controller 能力。
- [x] 1.2 创建 `docs/sql/growth.sql`，覆盖生长任务、生长尝试、生长锁、最近失败输入等系统事实结构。
- [x] 1.3 校验 `docs/api/growth.yaml` 与 `docs/sql/growth.sql` 均能回溯到本变更 spec、`docs/design/domain/枝化生长领域模块设计文档.md` 和 Agent 架构约束。
- [x] 1.4 对齐 Agent 侧枝化生长输入输出契约，明确 Growth 模块需要传递的任务上下文、授权引用范围和需要消费的候选果实输出形态。
- [x] 1.5 确认枝化生长 Skill、Agent 所需 Tool、AgentPort 枝化生长入口由 Agent 侧变更提供，本变更不实现这些 Agent 可执行能力。

## 2. 模块骨架与领域边界

- [x] 2.1 创建后端 `growth` 模块目录结构，符合后端开发规范的领域、应用、端口、适配器和控制器分层。
- [x] 2.2 定义枝化生长领域类型，覆盖来源节点引用、任务状态、内部尝试结果、生长锁、授权引用、失败输入和任务摘要。
- [x] 2.3 定义 Growth 应用服务入口，确保控制器不直接访问数据库、文件系统、AgentPort 或果实存储。
- [x] 2.4 定义生长任务存储端口、生长锁端口、引用资源校验端口、来源节点校验端口和时间/身份生成等基础端口。

## 3. 存储与锁适配

- [x] 3.1 实现内存生长任务存储适配器，用于服务测试和快速闭环。
- [x] 3.2 实现 SQLite 生长任务存储适配器，并与 `docs/sql/growth.sql` 保持一致。
- [x] 3.3 实现生长锁获取、查询和释放能力，确保同一来源节点不能重复生长。
- [x] 3.4 实现内部尝试记录能力，记录每次 AgentPort 调用和果实创建交付的成功或失败结果。
- [x] 3.5 实现最近失败输入保存与读取能力，仅保留来源节点最近一次零成功失败任务的可重试输入。

## 4. 校验与授权

- [x] 4.1 实现来源节点校验流程，支持种子和果实作为生长来源，并拒绝不存在、越界或已归档种子范围的来源。
- [x] 4.2 实现生成器校验流程，要求生成器必选且可用，拒绝不存在或已停用生成器。
- [x] 4.3 实现果实数量校验流程，未指定时使用默认值 3，指定时限制在 1 到 6。
- [x] 4.4 实现营养引用和基因引用授权校验端口，第一期允许通过真实模块或测试替身接入。
- [x] 4.5 确保授权包只包含受控引用范围，不把真实文件路径作为前端可控输入传给 AgentPort。

## 5. 生长任务执行

- [x] 5.1 实现发起枝化生长流程，完成校验后创建生成中任务并锁定来源节点。
- [x] 5.2 实现按果实数量串行执行内部生成尝试，每次尝试调用 Agent 侧提供的枝化生长入口。
- [x] 5.3 实现 AgentPort 输入适配，将来源节点、用户输入、生成器引用、授权资源范围和尝试目标组装为任务上下文。
- [x] 5.4 确保第一期不要求也不传递突变概率、杂交或高级生长策略参数。
- [x] 5.5 实现 AgentPort 失败、空输出、不可落地输出的尝试失败处理，并继续执行剩余尝试。

## 6. 果实交付与任务状态

- [x] 6.1 实现 Agent 输出到果实候选的适配与校验，拒绝空 Markdown 或无法落地的候选内容。
- [x] 6.2 调用果实领域内部 `createFruitFromCandidate` 能力创建候选果实，并挂载到生长来源节点下。
- [x] 6.3 实现部分成功不回滚规则，至少一个果实成功创建时将任务标记为已完成。
- [x] 6.4 实现零成功失败规则，没有任何果实成功创建时将任务标记为失败并保存最近失败输入。
- [x] 6.5 确保任务已完成、失败和异常路径都会释放来源节点生长锁。

## 7. 重试与查询能力

- [x] 7.1 实现重试最近失败任务能力，基于来源节点最近一次失败输入重新创建并执行生长任务。
- [x] 7.2 实现查询单个生长任务状态能力，返回生成中、已完成或失败状态，并在完成时返回成功创建的果实引用。
- [x] 7.3 实现查询来源节点生长状态能力，基于进行中任务或生长锁派生节点是否正在生长。
- [x] 7.4 实现查询来源节点最近失败输入能力，用于前端恢复悬浮输入框内容。

## 8. HTTP 控制器与应用装配

- [x] 8.1 创建 Growth HTTP Controller，暴露与 `docs/api/growth.yaml` 对齐的公开应用接口。
- [x] 8.2 更新应用启动装配，将 GrowthService、存储适配器、校验端口、AgentPort 和 FruitService 接入后端应用。
- [x] 8.3 确保 Growth Controller 不提供工作区整棵树查询能力，该能力留给工作区查询服务聚合。
- [x] 8.4 确保 Growth 模块不直接读写 Markdown 文件、不直接调用 LLM SDK、不绕过 AgentPort 或 FruitService，且不实现枝化生长 Skill 或 Agent Tool。

## 9. 测试与验证

- [x] 9.1 覆盖从种子和果实发起枝化生长成功创建任务的服务测试。
- [x] 9.2 覆盖来源节点不存在、越界、已归档种子范围、缺少生成器、生成器不可用、数量超过上限的拒绝测试。
- [x] 9.3 覆盖引用资源不可访问时拒绝创建任务，引用资源可访问时进入 AgentPort 授权包的测试。
- [x] 9.4 覆盖同一来源节点生长锁拒绝重复生长、其他节点不受影响、任务结束和异常路径释放锁的测试。
- [x] 9.5 覆盖数量为 5 时执行 5 次串行 Agent 调用，且每次调用符合 Agent 侧枝化生长输入契约的测试。
- [x] 9.6 覆盖 Agent 输出成功创建果实、Agent 输出不可落地、FruitService 创建失败的尝试结果测试。
- [x] 9.7 覆盖全部成功、部分成功不回滚、全部失败保存最近失败输入的任务状态测试。
- [x] 9.8 覆盖重试最近失败任务、没有失败输入时拒绝重试、查询任务状态、查询节点生长状态和查询最近失败输入的测试。
- [x] 9.9 覆盖 Growth Controller 与 `docs/api/growth.yaml` 对齐的接口测试。
- [x] 9.10 执行类型检查、Lint、测试和构建，确认枝化生长模块符合后端开发规范。
