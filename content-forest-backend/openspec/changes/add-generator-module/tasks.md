## 1. 顶层契约文档

- [x] 1.1 在 `docs/sql/` 创建或更新生成器系统事实对应的 SQL 契约文档，明确生成器身份、展示信息、启用状态、内容位置和追溯所需的存储结构。
- [x] 1.2 在 `docs/api/` 创建或更新生成器 Controller 对应的 OpenAPI 契约文档，覆盖导入、列表、详情、启用、停用、重新上传和可选生成器查询能力。
- [x] 1.3 对齐生成器领域文档、架构文档、后端规范、OpenSpec specs、SQL 契约和 API 契约，确认不存在字段职责或模块边界冲突。

## 2. 模块与端口骨架

- [x] 2.1 创建生成器领域模块结构，并以应用服务作为生成器业务能力入口。
- [x] 2.2 定义生成器模块所需的系统事实存储端口，避免业务模块直接依赖数据库适配器。
- [x] 2.3 定义或扩展内容访问端口，使其支持生成器 Skill zip 保存、读取、替换和合法性校验所需能力。
- [x] 2.4 将生成器模块注册到后端应用装配流程中，保持模块化单体边界清晰。

## 3. 内容访问与运行时文件系统

- [x] 3.1 实现生成器 zip 解压保存到运行时生成器内容目录的内容访问适配能力。
- [x] 3.2 实现 `SKILL.md` 存在性和可读取性校验。
- [x] 3.3 实现解压路径安全校验，拒绝绝对路径和运行时内容根目录之外的路径。
- [x] 3.4 实现生成器 Skill 本体读取能力，供详情查看和枝化生长后续读取使用。
- [x] 3.5 实现生成器 Skill 本体替换能力，并保证替换失败时不破坏原可读内容。

## 4. 存储与应用服务

- [x] 4.1 实现生成器系统事实持久化能力，并与 `docs/sql/` 中的契约保持一致。
- [x] 4.2 实现导入生成器应用服务，完成输入校验、内容保存、系统事实创建和默认启用。
- [x] 4.3 实现生成器列表与详情应用服务，支持查看启用和停用生成器。
- [x] 4.4 实现启用与停用应用服务，确保停用不删除内容本体、不破坏历史追溯。
- [x] 4.5 实现重新上传应用服务，确保新 Skill 合法后再替换，并保持生成器身份不变。
- [x] 4.6 实现可选生成器查询应用服务，只返回启用生成器，并提供枝化生长读取 Skill 所需依据。
- [x] 4.7 为文件保存成功但系统事实写入失败的场景补齐一致性处理或补偿策略。

## 5. HTTP 接口

- [x] 5.1 实现生成器导入接口，并与 `docs/api/` 中的上传契约保持一致。
- [x] 5.2 实现生成器列表、详情、启用、停用、重新上传和可选生成器查询接口。
- [x] 5.3 为参数校验失败、资源不存在、非法 zip、缺少 `SKILL.md`、内容访问失败等场景提供统一错误映射。
- [x] 5.4 确认 Controller 只负责请求解析、响应组装和错误映射，不直接访问数据库、文件系统或 Agent Runtime。

## 6. 测试与验证

- [x] 6.1 为导入生成器成功、缺少名称描述、非法 zip、缺少 `SKILL.md` 编写模块测试。
- [x] 6.2 为列表、详情、启用、停用、重新上传和可选生成器查询编写模块测试。
- [x] 6.3 为内容访问适配器编写路径越界、绝对路径拒绝、Skill 读取和替换失败保护测试。
- [x] 6.4 为接口层编写契约级测试或集成测试，确认行为与 `docs/api/` 对齐。
- [x] 6.5 运行后端类型检查、Lint、测试和构建，确认生成器模块符合后端开发规范。
